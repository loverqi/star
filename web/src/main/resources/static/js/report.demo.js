/** 
 * 报表模块测试工作空间js
 * */
function view(id, wechat_number, sta) {
	alert("显示方法测试，参数：[" + id + "," + wechat_number + "," + sta + "]");
}

function edit() {
	alert("编辑方法测试");
}

function deleted() {
	alert("删除方法测试");
}